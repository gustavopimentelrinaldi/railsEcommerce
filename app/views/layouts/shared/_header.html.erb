<!-- Navigation -->
<nav class="navbar navbar-inverse navbar-fixed-top" style="opacity: 0.9; border-radius:0;">
        <div class="container">
          <!-- Brand and toggle get grouped for better mobile display -->
          <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
              <span class="sr-only">Toggle navigation</span>
              <span class="icon-bar"></span>
              <span class="icon-bar"></span>
              <span class="icon-bar"></span>
            </button>
            <li>
                <%= link_to "Rinaldi Ecommerce", root_path, class:"navbar-brand" %>
            </li>
          </div>
      
          <!-- Collect the nav links, forms, and other content for toggling -->
          <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
            
            <ul class="nav navbar-nav navbar-right">
              <% unless member_signed_in? %>
              <li>
                <%= link_to "Entrar como Membro", new_member_session_path, role: "button" %>
              </li>
              <li>
                  <%= link_to "Cadastrar como Membro", new_member_registration_path, role: "button" %>
                </li>
              <li>
                  <%= link_to "Backoffice", backoffice_path, role: "button" %>
              </li>
              <% else %>
              <li>
                  <%= link_to "Perfil", profile_dashboard_path %>
              </li>
              <li>
                <%= link_to current_member.email %>
              </li>
              <li>
                  <%= link_to "Sair", destroy_member_session_path, method: :delete, role: "button" %>
              </li>
              <% end %>
            </ul>
          </div><!-- /.navbar-collapse -->
        </div><!-- /.container-fluid -->
      </nav>